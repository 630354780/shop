export declare type LessImportAliasPluginOptions = {
	/**
	 * The value `~` to flag if this plugin work on this less file.
	 * @default `~`
	 */
	prefix?: string;
	/**
	 * The root working directory
	 * @default `process.cwd()`
	 */
	projectCwd?: string;
	/**
	 * The user defined aliases.
	 * Note: alias replacement should be absolute path or relative to `projectCwd`
	 * @default {`@`: `join(process.cwd(), "src")`}
	 */
	aliases?: Record<string, string>;
};
/**
 * Custom file manager for less to support webpack style aliases.
 * This file manager loads the requested file and replaces instances of aliased imports before returning the file
 * contents for less to parse.
 */
export declare class LessImportAliasPlugin {
	private options;
	constructor(options: LessImportAliasPluginOptions);
	install(_less: any, pluginManager: any): void;
	minVersion: [
		3,
		0,
		0
	];
}

export {};
